<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bds.armory">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature android:name="android.hardware.camera" />

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Requests" />
        <activity android:name=".SendRequests" />
        <activity android:name=".PoliceAllocation" />
        <activity android:name=".DeclareWeaponSubmitDelay" />
        <activity android:name=".NonReturnedWeapon" />
        <activity android:name=".ReturnedWeapon" />
        <activity android:name=".SearchAllocation" />
        <activity android:name=".SearchPolice" />
        <activity android:name=".Assign" />
        <activity android:name=".Signin" />
        <activity android:name=".MainActivity">
        </activity>
        <activity android:name=".BarcodeCaptureActivity" />
    </application>

</manifest>